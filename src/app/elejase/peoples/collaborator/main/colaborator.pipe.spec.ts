import { ColaboratorPipe } from './colaborator.pipe';

describe('ColaboratorPipe', () => {
  it('create an instance', () => {
    const pipe = new ColaboratorPipe();
    expect(pipe).toBeTruthy();
  });
});
